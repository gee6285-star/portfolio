# 프로젝트 명

임성아 포트폴리오 웹사이트 (기업 지원용)

---

## 배경

너는 이 프로젝트를 위해 일하는 최고 수준의 프론트엔드 개발자야. 상명대학교 그린스마트시티학과 임성아 학생의 **기업 지원용 포트폴리오 웹사이트**를 만들기 위해 일해.

결과물은 누구나 쉽게 알아보고 이해할 수 있으면서, 동시에 프로페셔널하게 보여야 해. 최종적으로는 PDF로 변환해서 페이지별로 나누어 관리할 예정이야.

---

## 규칙

- 항상 한국어로 답한다.
- 코드는 나중에 수정하기 쉽게 작성한다.
  - 섹션/컴포넌트 단위로 구조를 명확히 나눈다.
  - 클래스명·id는 의미를 알 수 있게 짓는다.
  - 콘텐츠(텍스트)와 스타일/구조를 최대한 분리한다.
  - 반복되는 구조(카드, 목록 등)는 패턴을 통일하고, 가능하면 데이터 배열에서 자동 생성한다.
- 모르는 내용은 추측하지 말고 먼저 물어본다.

---

## 하지 말 것

- 확인되지 않은 프로젝트/역할/성과/수치/자격증 등을 임의로 지어내지 않는다.
- 사용자가 요청한 범위 밖의 내용을 마음대로 바꾸지 않는다.
- 기관명·데이터셋명이 정해지지 않았는데 임의로 만들어 쓰지 않는다.
- 큰 작업을 한 번에 다 바꾸지 않는다 — 확인 가능한 단위로 나눠서 진행한다.

---

## 참고 정보

- 이름: 임성아 / 학교: 상명대학교 / 학과: 그린스마트시티학과 / 학번: 202421389
- 프로젝트는 주로 그린스마트시티학과 관련 내용 (스마트시티, 도시 인프라, 토지정보, 공간정보, 환경)
- 전공 용어 기준이 될 기관 용어집: **아직 정하지 않음**
- 사용 데이터(토지정보 등)의 구체적 출처: **아직 정하지 않음**
- 그 외 확인이 더 필요한 항목: 자기소개 문장, 프로젝트명·기간·역할·내용, 수강 과목, 자격증/수상, 진로 목표
- **메인 웹페이지: [frontend/portfolio_1.html](./frontend/portfolio_1.html)** (사용자가 확정함, 2026-09-15)
- 관련 파일: [frontend/index.html](./frontend/index.html)(이전에 쓰던 대체 디자인, 현재는 사용 안 함), [portfolio_summary.md](./portfolio_summary.md)(파일 정리본)
- 2026-09-22부터 **프론트엔드([frontend/](./frontend/))/백엔드([backend/](./backend/))로 분리됨.** 자세한 내용은 아래 "완료된 작업" 참고.
- **관리자 페이지: [frontend/admin.html](./frontend/admin.html)** (2026-09-22 신설, 비밀번호 로그인 필요) — 프로젝트를 여기서 입력하면 공개 사이트에 자동 반영됨. 자세한 내용은 아래 "완료된 작업" 참고.
- 루트에서 `npm run dev` 하면 프론트엔드(5500)+백엔드(4000)가 한 번에 실행됨 (`package.json` 참고)

---

## 완료된 작업

### 프로젝트 카드 + 클릭 시 상세정보 열기 (portfolio_1.html, index.html)

"04. 프로젝트" 섹션을 표(table) 대신 카드로 바꾸고, 카드를 클릭하면 상세정보(기간/역할/내용/데이터)가 열리도록 함. `PROJECTS` 배열만 채우면 카드가 자동 생성됨.

### 프로젝트 분야별 필터 (portfolio_1.html)

"전체" + `PROJECTS`의 `tag` 값에서 자동으로 만든 필터 버튼을 추가함. 버튼을 누르면 해당 분야 카드만 보이고, 카드가 없으면 "해당 분야의 프로젝트가 아직 없습니다." 안내 문구가 뜬다. 헤드리스 브라우저로 전체/특정 분야/없는 분야 세 경우 모두 스크린샷으로 확인 완료.

- index.html에는 아직 필터를 넣지 않음 (portfolio_1.html이 메인으로 확정되어 우선순위가 낮아짐)

### 프론트엔드/백엔드 분리 (frontend/, backend/ 신설)

- `portfolio_1.html`, `index.html`을 `frontend/` 폴더로 이동함 (내용 변경 없음, 위치만 이동). 상대경로로 참조하는 이미지/폰트가 없어서 경로 문제 없음.
- `backend/`에 Node.js + Express 서버를 새로 만듦. **아직 DB에는 연결되지 않은 상태**이고, 지금은 `backend/src/data/projects.data.js`의 인메모리 배열로 동작함.
  - API: `GET /api/projects`(목록), `GET /api/projects/:id`(상세), `GET /health`(상태 확인)
  - 나중에 DB를 연결할 때는 `backend/src/services/projects.service.js` 안의 구현만 바꾸면 되도록 라우트/서비스/데이터 계층을 분리해둠 (자세한 순서는 `backend/README.md` 참고)
  - `npm install` → `npm start`로 실행 확인 완료 (실제로 서버를 띄워서 `/health`, `/api/projects` 응답 확인함)
- `frontend/portfolio_1.html`의 프로젝트 카드 로직 수정: 기존 하드코딩된 `PROJECTS` 배열은 `FALLBACK_PROJECTS`라는 이름으로 남겨두고, 화면이 열리면 `backend`의 `/api/projects`를 먼저 호출하도록 함. 백엔드 서버가 켜져 있으면 그 데이터를 쓰고, 꺼져 있거나 오류가 나면(정적 파일로만 열어도) 자동으로 `FALLBACK_PROJECTS`를 그대로 사용해서 화면이 깨지지 않음 — PDF 변환처럼 서버 없이 파일만 여는 경우에도 정상 동작.
- `index.html`은 이번 작업 대상에서 제외함 (이미 사용 안 하는 파일이라 API 연동 코드 추가 안 함).
- 루트에 `package.json` 신설: `npm install` → `npm run dev`로 backend(4000)+frontend(5500)를 한 번에 실행 (`concurrently` 사용).

### 관리자 페이지 (frontend/admin.html 신설) + 프로젝트 초안/공개 관리

**요청 배경**: 파일을 계속 업로드하는 대신, 관리자 화면에서 프로젝트를 직접 입력·수정하고 싶다는 요청(2026-09-22).

- **로그인**: `frontend/admin.html`에서 비밀번호로 로그인. 비밀번호는 bcrypt 해시로만 서버에 저장되고(`backend/.env`의 `ADMIN_PASSWORD_HASH`), 로그인 성공 시 JWT를 httpOnly 쿠키로 발급해 이후 요청을 인증함 (쿠키는 자바스크립트로 못 읽어서 XSS로부터 보호됨). 비밀번호 원문은 어디에도 저장 안 함.
  - 2026-09-22: 사용자가 본인 비밀번호로 교체 완료 (검증용 임시 비밀번호는 더 이상 사용 안 함). 비밀번호를 다시 바꾸는 방법은 `backend/README.md` 참고.
- **관리 항목**: 제목, 분야(태그, 기존 필터 기능과 연결됨), 내가 한 역할, 설명, 날짜, 참여 인원 수, 참고사항(선택), 프로젝트 자료 링크, 공개 상태(초안/공개)
- **빈칸 예시 문구**: 각 입력칸에 `placeholder`로 옅은 색 예시를 넣어두고, 입력 시 자동으로 사라지도록 함(HTML 기본 동작)
- **저장 규칙**: 참고사항을 제외한 모든 항목은 "공개" 선택 시 필수 — 프론트엔드(즉시 안내)와 백엔드(최종 검증, 400 에러) 양쪽에서 같은 규칙으로 확인함. "초안"은 제목만 있어도 저장 가능.
- **공개/초안 반영**: "공개"로 저장한 프로젝트만 공개 사이트(`GET /api/projects`)에 나타남. "초안"은 관리자 화면(`GET /api/admin/projects`, 로그인 필요)에서만 보이고 공개 사이트에는 안 보임.
- **데이터 저장**: DB가 아직 없어서 `backend/src/data/projects.json` 파일에 저장됨 (`projectsStore.js`가 읽기/쓰기 담당). DB 연결 시 이 파일만 교체하면 되도록 계층 분리해둠 (자세한 내용은 `backend/README.md`).
- 기존 예시 3건(캠퍼스 에너지 안내 등)은 실제 프로젝트가 아니라서 **모두 "초안" 상태로 시드**해둠 → 공개 사이트에는 아직 프로젝트가 0건으로 보임 (관리자 화면에서 실제 내용으로 채우고 "공개"로 바꿔야 사이트에 나타남).
- **검증**: Playwright로 전체 플로우 실제 실행 확인 완료 — 로그인 전 관리자 API 401 차단, 틀린 비밀번호 거부, 로그인 후 초안 3건 조회, 필수항목 누락 시 공개 차단(어떤 항목이 비었는지 안내), 모든 항목 채워 공개 저장 → 공개 사이트에 실제로 카드가 나타나는 것까지 스크린샷으로 확인.
- 저장 성공 메시지가 `resetForm()`에 의해 바로 지워지던 버그 발견 후 수정함.

### 초안/공개 선택지 개선 + 재접속 시 데이터 유지 검증 + 삭제 기능 (admin.html, backend)

- **재검증(2026-09-22)**: 새 브라우저 세션으로 재로그인해도 이전에 입력한 초안 내용이 그대로 남아있는지, 초안/공개 여부에 따라 공개 사이트 노출이 실제로 달라지는지 Playwright로 확인 완료 (전체 입력한 초안 / 제목만 입력한 초안 둘 다 정상 복원, 공개 전환 시 공개 사이트에만 반영).
- **초안/공개 선택지 시각 개선**: 선택된 쪽 박스에 강조 색(테두리+배경)이 들어가도록 CSS 추가 — 이전에는 라디오 버튼 자체 외엔 선택 여부가 잘 안 보였음.
- **DELETE /api/admin/projects/:id 신설**: 관리자 화면(전체 목록 + 중복 의심 패널)에서 프로젝트를 삭제할 수 있음 (삭제 전 확인창 뜸).

### 프로젝트 중복 확인 (backend/src/services/duplicateCheck.js, admin.html)

- **판단 기준(단순하게 유지)**: "제목"이 사실상 같으면(앞뒤 공백·대소문자·중간 공백 차이 무시) 중복으로 봄. 그 외 기준(설명 유사도 등)은 추가하지 않음.
- **동작 방식**: 자동으로 지우거나 합치지 않고, 관리자 화면에 "중복 의심 프로젝트" 패널을 만들어서 같은 제목의 항목을 나란히 보여줌 → 관리자가 직접 비교해서 "수정"(내용 통합) 또는 "삭제"로 정리.
- 새로 저장/수정 직후에도 같은 규칙으로 확인해서, 중복이면 저장 성공 메시지에 경고 문구가 추가로 붙음.
- API: `GET /api/admin/projects/duplicates`(중복 그룹 조회), `POST`/`PUT` 응답에 `hasDuplicate` 필드 포함.
- **검증**: API 레벨(중복 생성→그룹 조회→삭제→정리 확인)과 브라우저 UI 레벨(패널 노출→삭제→패널 사라짐) 둘 다 실제로 실행해서 확인. 테스트 중 만든 항목은 모두 삭제해서 사용자가 실제로 입력한 프로젝트("캠퍼스 내 조경설계" 등)에는 영향 없음을 확인함.

### 관리자 로그인 - 매번 새로 로그인하도록 강화 (보안 요청, admin.html, backend)

**요청 배경**: 창을 닫아도 로그인 기록이 남아있지 않고, 새 창을 열어도 항상 비밀번호를 다시 입력하게 해서 보안을 높이고 싶다는 요청(2026-09-22).

- 로그인 쿠키를 **세션 쿠키로 변경** (만료 시간을 지정하지 않음) → 브라우저를 완전히 종료하면 자동으로 사라짐.
- `frontend/admin.html`은 이제 페이지를 열 때마다(새 탭/새 창 포함) **기존 로그인 쿠키를 서버에 요청해서 무조건 지우고** 로그인 화면부터 보여줌. 즉 브라우저를 안 껐어도 관리자 페이지를 다시 열면 항상 비밀번호를 새로 입력해야 함 (기존에는 쿠키가 있으면 자동으로 대시보드로 들어갔음).
- 로그인 입력창에 `autocomplete="off"` 적용해서 브라우저가 비밀번호를 기억/자동완성하지 않도록 함 (다만 브라우저 자체 설정에 따라 완전히 보장되진 않음 — 확실히 하려면 브라우저의 "비밀번호 저장" 기능을 꺼두는 게 좋음, `backend/README.md`에 안내함).
- 프로젝트 전체(코드/문서)에 비밀번호 원문이 남아있지 않은지 검색해서 확인함 (해시만 `.env`에 저장되고, `.env`는 git에 안 올라감).
- **검증**: Playwright로 "같은 브라우저에서 새 창을 열어도 로그인 화면이 뜨는지", "쿠키가 실제로 세션 쿠키인지(만료시간 없음)", "새 창을 열면 기존 창도 새로고침 시 로그인 화면으로 돌아가는지", "쿠키 없이 서버에 직접 확인해도 401인지"까지 전부 실제로 실행해서 확인함.

### 자기소개 · 전공 소개 · 보유 역량 · 향후 계획 반영 (portfolio_1.html)

index.html에 이미 작성돼 있던 문장/태그를 사용자가 참고해서 쓰라고 확인해줘서(2026-09-15) portfolio_1.html의 01, 02, 03, 07 섹션에 그대로 반영함. placeholder 스타일(점선 박스)이 아니라 "확정된 정보" 스타일(실선 박스/태그, `.content-box`, `.tag-row`)로 바꿔서 legend와 일치시킴.

---

## 현재 작업과 완료 조건

**현재 작업**: 없음 — 다음 작업 대기 중

### 확인이 필요한 것 (다음 작업을 위해)

- [x] ~~관리자 비밀번호를 본인 것으로 교체~~ → 2026-09-22 완료
- [ ] 실제 프로젝트 내용 — 관리자 페이지(`frontend/admin.html`)에서 직접 입력하면 됨. 기존 예시 3건은 전부 "초안" 상태라 지금 공개 사이트에는 프로젝트가 0건으로 보임 (사용자가 입력 중인 "캠퍼스 내 조경설계" 1건 있음, 아직 초안 상태)
- [ ] index.html을 계속 보관할지, 정리(삭제)할지
- [ ] DB 종류(MySQL/PostgreSQL/MongoDB 등)와 접속 정보 — 정해지면 `backend/README.md`의 안내대로 연결
- [ ] 관리자 페이지 배포 시 주소 변경 필요 — 지금 `admin.html`, `portfolio_1.html`의 `API_BASE_URL`이 `http://localhost:4000`으로 하드코딩돼 있음 (로컬 개발용)
